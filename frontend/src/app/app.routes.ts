import { Routes } from '@angular/router';
import { InterviewMonitorComponent } from './interview-screen/interview-screen';

export const routes: Routes = [
    { path: '', component: InterviewMonitorComponent }
];
